{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start BG Key Desk",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app:app", "--host", "127.0.0.1", "--port", "8787", "--reload"],
      "jinja": true,
      "cwd": "${workspaceFolder}"
    }
  ]
}
